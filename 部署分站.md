# AgentAPI 分站部署手册

本文说明如何部署一个 AgentAPI 代理分站，以及主站“一键开分站”生成部署包后如何完成上线。分站是独立的 AgentAPI 服务，用户映射、模型请求和账务仍由 Sub2API 主站负责。

## 一、部署前准备

每个分站需要准备以下信息：

| 配置 | 示例 | 说明 |
| --- | --- | --- |
| 分站编号 | `agent01` | 全局唯一，生产后不要复用 |
| 分站域名 | `agent01.cc2.cx` | DNS 必须指向分站入口 |
| 分站名称 | `代理 01` | 页面和邮件中的品牌名称 |
| 主站地址 | `https://api.cc2.cx` | Sub2API 主站 Origin |
| 主站 Owner 用户 ID | `42` | 该分站的主站余额归属账户 |
| AgentAPI 镜像 | `registry.example/agentapi:v1` | 建议固定版本号，不要直接使用 latest |

服务端还需要四类秘密：

- `SUB2API_ADMIN_KEY`：AgentAPI 服务端调用主站管理接口；
- `SUB2API_APP_CREDENTIAL`：AgentAPI 服务端调用主站 `/v1`；
- `SUB2API_SSO_SECRET`：必须与 Sub2API 完全一致，至少 32 字节；
- `SESSION_SECRET`：当前分站独立的 Session 密钥，至少 32 字节。

这些值只能通过受保护的环境变量、Docker Secret 或 Secret Manager 注入，不能写入前端、镜像、URL、日志或返回给分站管理员。

## 二、DNS 和 Nginx

### 1. DNS

将 `agent01.cc2.cx` 解析到 Nginx 或统一入口服务器。生产环境建议使用：

- `*.cc2.cx` 通配符 DNS；
- 覆盖通配符域名的 TLS 证书；
- 入口层按 `Host` 转发到对应 AgentAPI 实例。

### 2. Nginx 配置

```nginx
server {
    listen 443 ssl http2;
    server_name agent01.cc2.cx;

    ssl_certificate     /etc/letsencrypt/live/cc2.cx/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/cc2.cx/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:18081;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Request-ID $request_id;
        proxy_read_timeout 120s;
    }
}
```

必须保留公开域名的 `Host`，否则 Cookie、Host 白名单和回调地址可能失效。AgentAPI 不负责申请证书，证书由 Nginx、Ingress 或 ACME 平台管理。

## 三、使用 Docker Compose 部署

以下命令在 `agentapi/` 目录执行。

### 1. 准备配置

```bash
cp backend/.env.example backend/.env
```

编辑 `backend/.env`，至少设置：

```env
MAIN_API_URL=https://api.cc2.cx
MAIN_MODEL_URL=https://api.cc2.cx

SUB2API_ADMIN_KEY=<主站管理员 Key>
SUB2API_APP_CREDENTIAL=<主站卫星应用凭据>
SUB2API_SSO_SECRET=<与主站相同的至少 32 字节密钥>
AGENTAPI_SSO_AUDIENCE=agentapi

SESSION_SECRET=<当前分站独立的随机密钥>
COOKIE_SECURE=true

AGENT_ID=agent01
AGENT_DOMAIN=agent01.cc2.cx
AGENT_NAME=代理 01
AGENT_SITE_NAME=代理 01 API
AGENT_SITE_LOGO=/logo.svg
AGENT_OWNER_MAIN_USER_ID=42
AGENT_ENABLED=true
AGENT_BILLING_MODE=owner_upstream
AGENTAPI_DATABASE_PATH=/app/data/agentapi.db
```

生产环境不要把真实密钥提交到 Git。`AGENT_OWNER_MAIN_USER_ID` 必须是主站真实用户 ID，不能填写邮箱或用户名。

### 2. 启动服务

```bash
export AGENTAPI_PORT=18081
docker compose up -d --build
docker compose logs -f agentapi
```

Compose 的默认安全设置包括：非 root 用户、只读根文件系统、受限 `/tmp`、丢弃 Linux capabilities 和 `no-new-privileges`。只允许 `/app/data` 持久化写入。

### 3. 验证容器

```bash
curl -fsS http://127.0.0.1:18081/healthz
curl -fsS http://127.0.0.1:18081/readyz
```

两个接口都应返回 HTTP `200`。`healthz` 只代表进程存活；`readyz` 还会检查数据库、Owner、应用凭据、管理员 Key 和 Session 密钥。

## 四、主站“一键开分站”流程

主站按钮使用现有 SSO 启动接口，不把管理员 Key 放进浏览器：

```text
主站登录用户
  → 点击 Toolbar “一键开分站”
  → GET /api/v1/auth/integrations/agentapi/start?next=/agent-admin
  → Sub2API 签发一次性 HMAC SSO 票据
  → AgentAPI /api/auth/sso/callback
  → AgentAPI 创建自己的 HttpOnly Session
  → 进入 /agent-admin
```

正式开站的编排顺序如下：

1. 校验操作者权限、slug、域名和幂等键；
2. 创建或确认主站 Owner 用户、额度和模型分组；
3. 生成该分站的 `AGENT_ID`、域名和独立配置；
4. 生成部署包、Compose 和 Nginx 配置；
5. 准备四类服务端秘密文件；
6. 启动 AgentAPI 容器；
7. 安装 DNS、TLS 和 Nginx 配置；
8. 检查 `/healthz`、`/readyz` 和公开域名访问；
9. 使用测试用户完成 SSO、注册、充值和小额模型请求；
10. 核对主站 usage 与 AgentAPI settlement 后，将状态改为 `active`。

## 五、生成部署包

当前镜像内置 `agentapi-provision`，也可以在源码目录执行：

```bash
cd agentapi/backend
go run ./cmd/agentapi-provision \
  --state-dir ../provisioning \
  --idempotency-key request-20260922-agent01 \
  --slug agent01 \
  --domain agent01.cc2.cx \
  --display-name "代理 01" \
  --owner-main-user-id 42 \
  --main-url https://api.cc2.cx \
  --image registry.example/agentapi:v1
```

该命令具备幂等键重放、slug 冲突保护和域名冲突保护，会生成：

- 每站独立的 Compose 文件；
- Nginx Host 配置；
- 非秘密 `.env`；
- 独立 Session Secret；
- 部署状态记录。

管理员 Key、App Credential 和 SSO Secret 只引用外部文件，不写进生成结果。命令输出 `generated` 只表示部署包已生成，不表示分站已经上线；必须完成启动、DNS、TLS、Nginx 和 `/readyz` 验收后才能标记 `active`。

## 六、上线验收

### 基础验收

- [ ] `https://agent01.cc2.cx/healthz` 返回 `200`；
- [ ] `https://agent01.cc2.cx/readyz` 返回 `200`；
- [ ] 浏览器访问公开域名时没有证书错误；
- [ ] 未知 Host 不会进入该分站；
- [ ] 容器不是 root，根文件系统保持只读；
- [ ] AgentAPI 数据库位于持久化卷。

### 业务验收

- [ ] 主站登录用户点击“一键开分站”可以进入 AgentAPI；
- [ ] SSO 票据只能使用一次，浏览器只保存 AgentAPI HttpOnly Session；
- [ ] 分站注册用户能在主站创建对应用户映射；
- [ ] 分站用户可创建 `sk-agent-*` 客户端 Key；
- [ ] 模型请求只使用公开模型名，并正确扣减分站可用额度；
- [ ] 主站 usage、AgentAPI settlement 和 request_id 可以对应；
- [ ] 充值回调重复提交不会重复增加额度；
- [ ] Owner 余额不足时，分站请求被拒绝且不会产生未授权扣费。

## 七、常见问题

### `/readyz` 返回 503

检查 `SUB2API_ADMIN_KEY`、`SUB2API_APP_CREDENTIAL`、`AGENT_OWNER_MAIN_USER_ID`、`SESSION_SECRET` 是否配置，数据库卷是否可写，以及 Owner 是否仍有效。

### SSO 返回 `SSO_NOT_CONFIGURED`

确认 Sub2API 和 AgentAPI 的 `SUB2API_SSO_SECRET` 完全一致，长度至少 32 字节，并确认主站配置了：

```env
agentapi_link=https://agentapi.cc2.cx
AGENTAPI_SSO_CALLBACK_URL=https://agentapi.cc2.cx/api/auth/sso/callback
```

修改后重启两个服务。

### 页面打开但登录后回到主站

检查 SSO 回调路径是否为 `/api/auth/sso/callback`、Nginx 是否保留 `Host` 和 `X-Forwarded-Proto`，以及 `COOKIE_SECURE` 是否与实际 HTTPS 状态匹配。

### settlement 长时间 pending

先同步 Owner 钱包，再执行 Agent 管理员对账接口。不要通过更换 `request_id` 重放原模型请求；主站 usage 查询不到时，系统应保持 pending，等待主站恢复后再对账。

## 八、停站、撤站和备份

停站时先从 Nginx 移除公开入口，再将 Agent 状态设置为 `suspended` 或 `revoked`，吊销 App Credential 和管理员 Key，完成 pending settlement 对账后再归档数据。不要直接删除数据库，也不要复用原 `AGENT_ID`。

SQLite 数据库应定期在线备份，备份内容必须加密保存。恢复时使用原 `SESSION_SECRET`，先检查 `/readyz` 和 Owner 额度，再开放外部流量。

## 九、相关文档

- `agentapi/10-部署域名与配置.md`
- `agentapi/19-上线与运维手册.md`
- `agentapi/04-一键开分站流程.md`
- `agentapi/05-主站接口契约.md`
- `管理员key接口文档.md`
- `docs/卫星应用接入公共层.md`
