# Presenton 对接文档整理计划

## 目标

生成 `操作手册.md` 与 `项目架构.md`，说明 Presenton 的启动、配置、模型能力和与 Sub2API 对接边界。

## 阶段

- [x] 探索项目结构、启动方式和现有容器
- [x] 汇总模型类型、环境变量、API 与数据流事实
- [x] 编写两份中文文档
- [x] 交叉检查文档准确性与可执行性

## 约束与决策

- 不把仓库中没有实现或没有配置入口的能力描述为已支持。
- API Key 只通过环境变量/配置注入示例说明，不写入源码。
- 文档使用简体中文，文件名按用户指定。

## 错误记录

- Docker Compose 构建因访问 Docker Hub 超时失败；已使用本机缓存的 Presenton 镜像启动验证。
- 首次文档补丁因 Markdown 反引号转义造成格式错误；已清理并通过反引号/代码围栏检查。

---

# Sub2API 模型适配方案整理计划

## 目标

依据 `模型适配.md` 和现有代码，形成 `模型适配实施方案.md`，覆盖图片、视频模型适配，Superkey 跨分组动态路由，以及 Canvas、QRCode、PPT 的统一接入和验收。

## 阶段

- [x] 读取需求文档并确认模型、上游和默认协议规则
- [x] 核实现有网关、适配器、路由与子应用调用链
- [x] 设计模型能力目录、Provider Profile、动态路由和任务归属
- [x] 编写分阶段实施、测试矩阵、风险与验收标准
- [x] 检查方案与代码事实一致，确认未臆造缺失协议

## 约束与决策

- 本轮只输出实施方案，不修改实现、不构建、不部署。
- 未特殊标注的模型默认采用 OpenAI-compatible 格式。
- 特殊平台协议必须由真实文档或请求/响应 fixture 驱动，不根据模型名称猜测。
- 对三个子应用维持稳定的统一 API，不把供应商差异泄漏到客户端。
