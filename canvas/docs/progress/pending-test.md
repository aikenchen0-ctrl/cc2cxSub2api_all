---
title: 待测试
description: 当前版本已实现但仍需人工验证的变更项
---

# 待测试

- 画布默认只生成 1 张图；只有在节点图像设置里手动改成多张后，才会按该数量生成。新建配置节点默认 count 为 1，不再读取全局旧的默认张数。
- 当前部署已统一为 canvas-app（canvas:workspace），由 sub2api/deploy/docker-compose.override.yml 从当前 canvas 目录构建，唯一端口为 3522。原 3522、3523、3524 及停止的预览备份容器已清理；旧镜像保留用于回退。
- 沿用 canvas_integrated_data 数据卷；三个预览数据副本及旧 3522 容器文件系统备份位于 sub2api/deploy/migration-backups/canvas-cleanup-20260918-132513。原 3522 丢失的数据目录未恢复。
- 当前源码镜像构建成功；Dockerfile 复用 Go 构建阶段的 CA 证书，避免 Debian 软件源 502 导致失败。3522 健康接口、设置数据库查询和画布/视频/登录页面通过；门户健康通过，回调已改为 3522，密钥一致。真实账号 SSO 和真实模型生成仍待验收。以下涉及 3523/3524 的记录为历史验证结果，不代表当前部署。
## Sub2API

- 已将真实 18080 门户升级为 sub2api:canvas-integration，保留原数据库、配置卷和环境变量；升级前导出 PostgreSQL dump 与配置归档并保留 sub2api:pre-canvas-deploy 回滚镜像。Canvas 最新镜像在 3524 由同一 Compose 管理，原预览数据迁入 canvas_integrated_data 持久化卷，配置与门户一致的 SSO 密钥和固定 JWT 密钥。门户回调为 http://localhost:3524/api/auth/sso/callback，旧 3522 故障容器未修改。两端健康检查通过，签发接口未登录返回 401，浏览器登录页/画布页渲染检查通过；真实账号点击验收与 Relay 模型生成仍待验证。
- 三个自定义视频模型的侧边设置面板已与请求共用时长选项及固定分辨率定义，显示归一化后的参数；14 项相关测试和独立 TypeScript 检查通过。镜像 canvas:video-compatibility 已构建并启动独立预览 http://127.0.0.1:3524/video。Chromium 验证三个模型的时长/固定分辨率选项、点击后时长与视频专用尺寸持久化均通过，无未处理 JS 异常，已保存截图。底部快捷布局和参考素材入口尚未对齐，未进行真实生成。3522 原部署未改动。
- 对照 canvas-old 恢复 OpenAI 兼容渠道下三个精确自定义模型的请求约定：seedance2.5-9图 固定 30 秒/720p，seedance2.0-900-720p 使用 10/15 秒，minimax-h3-933-图文 使用 5/10/15 秒和 768p 尺寸。保留旧 JSON 图片字段、最多 8 张参考图及 Vivid 直连格式，其他协议不受影响；不支持的首尾帧/音视频参考明确报错。新增 8 项回归测试通过，相关前端测试共 13 项通过，独立 TypeScript 检查通过。源码修复尚未重新构建到运行容器，未进行真实上游生成验收。
- 新增 router/sub2api_video_flow_test.go：通过真实路由和后台轮询器，验证 multipart 参考图、时长与分辨率字段转发、任务创建、轮询完成、状态查询、授权 content 下载及禁止缓存。单项和 Go 全量测试通过。上游为 HTTP 测试服务，仅作协议回归，不代表真实模型生成验收。
- 新增环境变量管理的 Relay 渠道：启动配置、密钥轮换、模型移除、停用清除密钥；保留其他渠道及明确配置的权限。
- Relay 请求保持网关协议，跳过按模型名匹配的原生厂商转换。
- 新增 SSO 接收、持久化防重放、独立普通账号、HttpOnly 会话交接及前端登录处理。
- 修复门户 fragment 凭据导入与 URL 清理，并更新实际本地渠道列表，保留旧查询参数入口。
- Canvas Go 全量测试、Sub2API Canvas SSO 签发单元测试及 fragment 导入测试已通过。新增 Relay 视频 content 鉴权下载及跨用户隔离 HTTP 测试通过；真实部署视频全链路与浏览器验收尚未完成。
- 门户新增 Canvas SSO 签发端、CANVAS_SSO_CALLBACK_URL 配置和默认 SSO 按钮；ju/livart 入口保持不变。
- 修复音频裁剪 Slider 回调类型；独立 TypeScript 检查、前端生产构建和完整 Docker 镜像构建通过，Canvas Go 全量测试再次通过。
- 独立测试容器在 http://127.0.0.1:3523 运行，首页、画布页、登录页、健康检查和设置接口均返回 HTTP 200。现有 3522 容器未替换。桌面浏览器控制连接不可用，已改用隔离 Chromium 无头浏览器进行实际交互测试。
- Chromium 实际页面测试通过：账号密码登录、点击新建画布、刷新恢复同一画布、未捕获到未处理的 JavaScript 异常；已保存 1440x1000 桌面与 390x844 手机视口截图，手机视口页面宽度未溢出。截图未进行人工视觉验收；门户 SSO 浏览器跳转与真实模型调用仍待验收。
- 隔离镜像 Chromium SSO 浏览器测试 8 项通过：真实回调自动交换会话并跳转画布、最终 URL 清除票据且交接 Cookie 删除、账户菜单显示身份、刷新恢复同一身份、SSO 用户新建画布、390px 页面不横向溢出、全新浏览器拒绝票据重放、无未处理 JavaScript 异常。已保存桌面与手机截图，临时容器已清理。票据由测试脚本签发，已部署 Sub2API 门户按钮和签发端尚未纳入本轮验收。
- 构建 sub2api:canvas-integration 镜像成功，前端类型检查、生产构建和 3 项 i18n 测试通过。在隔离 PostgreSQL/Redis、真实门户和 Canvas 镜像上，用普通用户完成 Chromium 门户登录、点击无限画布、真实签发接口、弹窗自动登录、清理 URL/交接 Cookie、断开 opener、创建画布及刷新恢复的完整链路，无未处理 JS 异常。注册默认关闭，普通账号由临时数据库测试数据创建；未代替用户接受管理员合规声明。测试容器和网络已清理。
- 已核实现有 18080 门户的 Canvas 签发接口返回 404，3522 Canvas 未配置 SSO 共享密钥；两者仍是原部署，尚未替换。隔离环境通过不代表现有部署或真实上游模型调用已验收。
- 独立容器实际 HTTP 验收 11 项通过：两账号注册、登录、错误密码拒绝、当前身份、画布保存与读取、跨账号隔离、未认证访问拒绝、容器重启后重新登录及画布持久化。测试仅使用独立容器数据，不代表 SSO 或真实模型调用验收通过。
- 使用独立随机共享密钥和测试身份，对实际运行镜像完成 18 项 SSO HTTP 检查：合法回调、HttpOnly/Strict Cookie 属性、URL 不含会话令牌、禁止缓存、跨站与缺少请求头拒绝、会话交换和 Cookie 清除、普通用户角色、无 Cookie 拒绝、签名与过期拒绝、重放拒绝、外部跳转归一化、同名不同 subject 隔离、重启后身份稳定及未过期票据防重放。此项不是门户签发或浏览器 Cookie 行为的端到端验收。
