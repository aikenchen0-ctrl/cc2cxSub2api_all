services:
  app:
    networks:
      - default
      - sub2api

networks:
  sub2api:
    external: true
    name: ${SUB2API_DOCKER_NETWORK:?Set SUB2API_DOCKER_NETWORK to the existing gateway network}
