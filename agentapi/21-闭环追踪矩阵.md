# 21 闭环追踪矩阵

本文把 AgentAPI 从“开站”到“撤站”的完整生命周期串起来，作为产品确认、开发实现、测试验收和上线运维之间的单一导航。每个业务动作都必须能回答四个问题：谁是事实来源、写入哪一份数据、失败后如何恢复、用什么验收。

## 1. 端到端业务闭环

```text
主站用户申请开站
  → 创建 Agent 与域名绑定
  → 创建/绑定主站代理管理员账户
  → 配置 Owner 扣费身份、模型范围和品牌
  → DNS/TLS/Nginx 就绪
  → Agent 健康检查通过并激活
  → 代理用户在 AgentAPI 注册
  → 主站创建真实用户，AgentAPI 保存映射
  → 代理管理员在主站充值
  → AgentAPI 同步 Owner 余额并分配用户子额度
  → 用户使用 AgentAPI Key 调用公开模型
  → 本地预授权 + 主站 Owner 唯一扣费
  → settlement 按 request_id/usage_id 对账
  → 钱包、用户用量和主站 usage 可追溯
  → 暂停、撤站、结算 pending 后归档
```

方案 A 的核心边界只有一句话：AgentAPI 的本地钱包负责预授权、分配和归属；Sub2API 主站的 `agent_owner_main_user_id` 负责模型请求的唯一真实扣费。

## 2. 生命周期追踪

| 阶段 | 事实来源 | AgentAPI 写入 | 主站动作 | 失败/恢复 | 验收依据 |
| --- | --- | --- | --- | --- | --- |
| 申请开站 | 主站控制面 | 主站 `agent_provisioning_agents`、幂等记录、状态事件和 worker lease hash | 主站管理员创建 `pending/queued`；worker claim 后租约保护 progress/activate | 相同管理员/幂等键重放返回同一记录；租约原子认领/续租，失租拒绝状态写入，状态变更事务写事件 | [04-一键开分站流程](04-一键开分站流程.md)、[05-主站接口契约](05-主站接口契约.md) |
| 域名就绪 | DNS/TLS/Nginx | `domain_bindings` | 不直接改主站业务账 | DNS、证书或 Host 校验失败则保持 `provisioning/failed` | [10-部署、域名与配置](10-部署域名与配置.md) |
| Agent 激活 | 外部部署检查 + 主站管理员确认 | `agent.status=active`、`domain_status=ready` | 管理员/后续 worker 调用 `/activate` 并确认 readiness；AgentAPI 只在权威 GET 为 active 后开放业务路由 | 未通过 `/readyz` 不应调用激活接口；控制状态不可读或过期则 AgentAPI fail closed | [05-主站接口契约](05-主站接口契约.md)、[11-状态机、幂等与故障恢复](11-状态机幂等与故障恢复.md) |
| 用户注册/登录 | Sub2API 公开认证 API 或现有 HMAC SSO 身份 | `agent_users(agent_id, main_user_id)`、本地 Session；主站 `agent_runtime_user_mappings` | `POST /agent-runtime/users/{id}/map` 必须同时通过逐站 control 凭据和匹配当前用户的 JWT / `agentapi` SSO ticket 证明 | 只允许绑定已验证身份；映射冲突/禁用账号拒绝；不能由 runtime control credential 指定任意账户 | [05-主站接口契约](05-主站接口契约.md)、[08-用户注册与模型请求](08-用户注册与模型请求.md) |
| 映射用户启停 | Agent Owner 操作 + Sub2API Agent Runtime API | 当前 Agent 映射状态与结构化审计 | 调用 `PATCH /api/v1/agent-runtime/users/{id}/status`，只发送 `status`；主站再次校验映射，该动作改变主站全局账号状态 | 禁用先关本地门，主站失败也 fail-closed；启用先等主站确认；Agent Owner 不可停用 | [18-实现与接口手册](18-实现与接口手册.md)、[12-测试验收清单](12-测试验收清单.md) |
| 代理充值 | 主站 Owner 钱包 | `wallet_sync` 快照和流水 | Owner 在主站完成充值 | 同步失败不增加本地余额；重试同一快照 | [09-支付与充值](09-支付与充值.md)、[17-账务权威与主站扣费桥接](17-账务权威与主站扣费桥接.md) |
| 额度分配 | AgentAPI 本地钱包 | `available/allocated/user_balance` 和流水 | 不改变主站余额 | 幂等键防止重复分配；超分配直接拒绝 | [07-预充值与账务](07-预充值与账务.md) |
| 模型请求 | 主站 `/v1` usage | 预授权、`settlements`、用户归属 | 以 `agent_owner_main_user_id` 只扣一次 | 明确失败释放；成功但 usage 不明进入 pending | [08-用户注册与模型请求](08-用户注册与模型请求.md)、[17-账务权威与主站扣费桥接](17-账务权威与主站扣费桥接.md) |
| 对账 | 主站 usage 查询 | settlement 确认/释放、审计记录 | 返回与 `request_id` 对应的 usage | 找不到 usage 不猜金额、不自动退款 | [18-实现与接口手册](18-实现与接口手册.md)、[19-上线与运维手册](19-上线与运维手册.md) |
| 暂停/撤站 | 平台运营状态 | `suspended/revoked`、归档记录 | 状态变更提交后经 SSE 提示托管实例重读；周期 GET 兜底，保留主站流水 | 非 active 即拒绝新业务；状态过期也 fail closed；继续完成 pending 对账，禁止复用身份 | [05-主站接口契约](05-主站接口契约.md)、[19-上线与运维手册](19-上线与运维手册.md) |

## 3. 需求到文档的映射

| 需求 | 设计边界 | 实现/接口 | 验收/运维 |
| --- | --- | --- | --- |
| 独立代理站和品牌 | [00-设计基线与决策](00-设计基线与决策.md)、[01-业务边界与目标](01-业务边界与目标.md) | `GET /api/v1/settings/public`、`/api/v1/agent/admin/branding`、[18-实现与接口手册](18-实现与接口手册.md) | [12-测试验收清单](12-测试验收清单.md) |
| 一键开站 | [02-总体架构与请求链路](02-总体架构与请求链路.md) | [04-一键开分站流程](04-一键开分站流程.md)、[05-主站接口契约](05-主站接口契约.md) | [13-实施顺序与风险](13-实施顺序与风险.md) |
| 用户归属 | [03-领域模型与数据结构](03-领域模型与数据结构.md) | `/api/v1/auth/*`、`agent_users` | [08-用户注册与模型请求](08-用户注册与模型请求.md) |
| 映射用户状态 | Agent Owner 管理当前 Agent 已映射用户 | AgentAPI `/api/v1/agent/admin/users/{main_user_id}/status` → Sub2API `PATCH /api/v1/agent-runtime/users/{id}/status` | [12-测试验收清单](12-测试验收清单.md)、[18-实现与接口手册](18-实现与接口手册.md) |
| 模型范围 | 主站公开模型目录 + AgentAPI 本地策略 + Sub2API model credential 持久化 scope | `/api/v1/agent/admin/model-policy` → `PUT /api/v1/agent-runtime/model-policy`；Sub2API `/v1` 请求校验与模型发现过滤 | [12-测试验收清单](12-测试验收清单.md)、[18-实现与接口手册](18-实现与接口手册.md) |
| 用量历史 | AgentAPI 本地 settlement + 同一 request ID 的主站 usage | `/api/v1/agent/usage?page=...` 与用户安全的主站 usage 快照 | [12-测试验收清单](12-测试验收清单.md)、[18-实现与接口手册](18-实现与接口手册.md)；legacy Owner 余额差只作明确标记的兼容来源 |
| 图片/视频任务恢复 | AgentAPI 本地 task 索引 + Sub2API 原任务查询 API | `/api/v1/agent/tasks?page=...` 仅返回当前 Session 用户记录；恢复操作复用原任务 ID 和 settlement | 页面重载恢复测试、分页与用户隔离测试；不重新创建任务，不读取主站数据库 |
| 无佣金、Owner 统一扣费 | [17-账务权威与主站扣费桥接](17-账务权威与主站扣费桥接.md) | `X-Sub2API-On-Behalf-Of`、settlement 接口 | [12-测试验收清单](12-测试验收清单.md)、[19-上线与运维手册](19-上线与运维手册.md) |
| 浏览器凭据安全 | [06-认证与安全](06-认证与安全.md) | HttpOnly Session、`sk-agent-*` | [15-客户端 API Key 与接口手册](15-客户端API Key与接口手册.md) |
| 充值和退款 | [09-支付与充值](09-支付与充值.md) | provider-neutral 订单/HMAC webhook/Owner 额度后分配已实现；真实渠道与退款未实现 | [12-测试验收清单](12-测试验收清单.md)、[14-待拍板事项](14-待拍板事项.md) |
| 域名和部署 | [10-部署、域名与配置](10-部署域名与配置.md) | Docker、Nginx、`/healthz`、`/readyz` | [19-上线与运维手册](19-上线与运维手册.md) |
| 失败恢复和审计 | [11-状态机、幂等与故障恢复](11-状态机幂等与故障恢复.md) | `request_id`、`order_id`、pending settlement | [12-测试验收清单](12-测试验收清单.md) |

## 4. 不变量与阻断条件

以下任一条件不满足，都不能把 Agent 状态标为 `active`，也不能开放外部注册：

1. `agent_id`、域名、Owner 主站账户和服务凭据是一一对应的；
2. 浏览器无法获得主站 JWT、逐站 runtime control/model credential、SuperKey 或主站用户 Key；
3. 每次模型请求都有唯一 `request_id`，重试不会再次访问主站；
4. 主站扣费身份固定为 `agent_owner_main_user_id`，客户端请求头不能覆盖；
5. 本地 `available + allocated + frozen` 不超过最近一次可验证的 Owner 主站余额；
6. 主站成功但 usage 不可确认时进入 `settlement_pending`，不得猜测金额或静默退款；
7. 主站通用 `/api/v1/*` 不通过 AgentAPI 任意透传；
8. 撤站时保留用户映射、钱包流水、settlement 和审计记录，不物理删除账务历史。

## 5. 当前闭环范围与明确缺口

当前参考实现已闭环到自动化/代码层：认证代理、用户映射、本地额度预授权、Owner 唯一扣费转发、pending settlement、手动与后台对账、文本流式处理、视频和异步图片任务归属/轮询/历史恢复、品牌、客户端 Key、健康检查和单实例 Docker 部署。真实 edge、供应商与支付渠道运营仍需按验收清单验证。

以下能力仍是后续阶段，不能在上线说明中写成“已完成”：

- 逐站 runtime control/model 凭据、用户绑定身份证明、Owner/usage/status 管理 API 和状态通知 SSE 已实现；共享 provisioning-only worker credential 仍有主站范围，并且不限制 Docker/Nginx 主机权限。租约保护主站状态所有权，但不能为 Docker/Nginx 副作用提供 fencing。worker 仍需在受控 edge 节点单独安装并做真实环境验收；失败任务须由管理员显式 retry，worker 不会自动重试；
- 真实支付供应商 SDK、渠道专属验签/支付页、退款、拒付和自动入账；provider-neutral HMAC 回调桥接已实现但默认关闭；
- 多 Agent 共享进程的 Host 路由；
- 视频与异步图片任务已完成创建归属、跨用户隔离、轮询、原 settlement 结算和后台超龄探测；失败任务先检查主站 usage，有实际费用则结算，权威确认无 usage 才释放，未知/不可用仍 pending。未纳入显式白名单的其他异步端点拒绝转发。文本 SSE 已支持 EOF/断流 settlement 处理。

这些缺口的实现顺序见 [13-实施顺序与风险](13-实施顺序与风险.md)，上线前阻断条件见 [16-实现状态与边界](16-实现状态与边界.md)。

## 6. 变更控制

任何涉及扣费身份、余额含义、公开模型名、Session TTL、请求头或 settlement 状态的变更，必须同时更新：

- 本文；
- [00-设计基线与决策](00-设计基线与决策.md)；
- [05-主站接口契约](05-主站接口契约.md)；
- [07-预充值与账务](07-预充值与账务.md)；
- [12-测试验收清单](12-测试验收清单.md)；
- [20-版本变更记录](20-版本变更记录.md)。

没有完成上述同步和回归验收，不允许只改代码或只改某一份接口文档。

## 7. 充值桥接追踪补充

| 业务动作 | 事实来源 | AgentAPI 写入 | 失败/恢复 | 验收依据 |
| --- | --- | --- | --- | --- |
| 创建充值订单 | Agent Session + 服务端支付配置 | `recharge_orders`（订单号、金额、用户、过期时间） | 幂等键复用返回原订单；金额/范围不合法拒绝 | `POST /api/v1/agent/recharge/orders`、[12-测试验收清单](12-测试验收清单.md) |
| 支付回调 | 供应商原始 body + HMAC 签名 | `payment_events`，订单进入 `paid_pending_allocation` | 签名/金额/币种/状态不匹配不写钱包；重复事件只读原结果 | `POST /api/v1/payments/webhook`、[09-支付与充值](09-支付与充值.md) |
| 用户余额分配 | 已同步的 Owner 主站余额 | 同一事务写入 `user_allocate`、`agent_allocate`，订单进入 `allocated` | 主站不可用或额度不足保留 `paid_pending_allocation`，管理员可重试 | `POST /api/v1/agent/admin/recharge/allocate`、[18-实现与接口手册](18-实现与接口手册.md) |
| 真实支付接入 | 具体渠道 SDK/商户合同 | 当前不写主站充值流水 | provider-neutral 桥接默认关闭；接入前补充退款/拒付/自动入账和对账 | [16-实现状态与边界](16-实现状态与边界.md)、[20-版本变更记录](20-版本变更记录.md) |
