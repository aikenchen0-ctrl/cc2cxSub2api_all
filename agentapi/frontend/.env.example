# AgentAPI is same-origin in production; the browser never receives a
# Sub2API access token, refresh token, SuperKey, or administrator key.
VITE_API_BASE_URL=/api/v1
VITE_DEV_PROXY_TARGET=http://localhost:8080
VITE_DEV_PORT=3000
