# Compose interpolation values (the service secrets live in backend/.env).
# Use a different host port when running more than one AgentAPI instance on
# the same machine; Nginx/Ingress can then route each public domain to it.
AGENTAPI_PORT=8080
# Must match the main Sub2API value. It is injected into the container only.
SUB2API_SSO_SECRET=
