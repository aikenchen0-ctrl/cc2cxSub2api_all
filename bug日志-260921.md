# Bug 日志：线上 SSO 登录失败（2026-09-21）

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-09-21 |
| 问题 | 本地环境可以通过 SSO 登录，部署到线上后提示 `SSO_failed` |
| 线上主机 | `38.175.201.140:22` |
| 线上项目目录 | `/opt/cc2cxSub2api_260920` |
| 本地项目目录 | `C:\Users\kodesh\Desktop\CC2CX` |
| 主要受影响服务 | PPT/Presenton；同时发现 Canvas、QRCode 的密钥配置也存在问题 |
| 最终状态 | 已修复并通过线上新鲜票据验证 |

> 安全说明：本记录不保存 root 密码、SSO 密钥原文、JWT、API Key 或用户密码哈希。线上验证只记录状态码和不可逆指纹。

## 2. 用户可见现象

从 Sub2API 已登录状态进入线上应用时，浏览器没有进入应用首页，而是回到登录错误页面，表现为：

```text
SSO_failed
```

本地 Windows 环境中的同一套 SSO 流程可以正常完成，因此初步判断不是票据格式或前端按钮逻辑问题，而是线上部署环境的配置、数据库或反向代理行为与本地不一致。

## 3. SSO 正常工作链路

SSO 链路由以下步骤组成：

1. Sub2API 为当前用户生成一次性签名票据。
2. 浏览器访问业务应用的 `/api/auth/sso/callback?ticket=...`。
3. 业务应用使用双方共有的 `SUB2API_SSO_SECRET` 校验票据签名、受众、有效期和一次性 ID。
4. 回调成功后，业务应用写入短时交接 Cookie。
5. 前端在同源条件下调用 `/api/.../auth/sso/exchange`。
6. 业务应用消费交接 Cookie，并签发自己的本地会话 Cookie。

任何一个环节失败，用户最终都会看到 SSO 登录失败；因此不能只检查 Sub2API 的签发接口，还必须检查接收应用的密钥、数据库、Cookie 和反向代理协议。

## 4. 排查过程与证据

### 4.1 连接线上并确认服务状态

通过 SSH 连接线上主机后，确认 Compose 项目和各应用容器均存在。初次排查重点放在 PPT，因为 PPT 的本地 SSO 测试已通过，而线上回调失败。

线上目录：

```text
/opt/cc2cxSub2api_260920
```

### 4.2 发现部分容器缺少共享 SSO 密钥

检查 `SUB2API_SSO_SECRET` 时发现：

- `canvas` 容器缺失该变量；
- `ppt` 容器缺失该变量；
- `qrcode` 容器缺失该变量。

这会导致接收端无法使用与 Sub2API 相同的 HMAC 密钥验证票据。即使票据由 Sub2API 正确签发，接收端也会将其视为无效票据并跳转到 `SSO_failed`。

补齐配置并重建后，所有相关容器的检查结果为：

```text
密钥长度：64
SHA-256 前缀：ab796955e751ccb2
```

这里只记录长度和指纹前缀，不记录密钥原文。

### 4.3 发现线上 PPT 没有管理员账户

线上 PPT 的认证状态最初为：

```json
{"configured": false}
```

数据库用户数为 0。PPT 的 SSO 实现有意禁止 SSO 用户占用首次管理员初始化的位置：

- 首次访问的 SSO 用户只能被视为普通用户；
- 管理员必须先完成本地初始化；
- 当数据库没有管理员时，SSO 回调会失败，而不是自动创建管理员。

因此，仅补齐共享密钥仍然无法解决线上 PPT 登录问题。

### 4.4 发现 PPT 数据库迁移版本落后

在线上执行管理员初始化和认证查询时出现：

```text
sqlite3.OperationalError: no such column: owner_id
```

原因是线上 `fastapi.db` 来自较早版本，数据库没有完成当前代码所需的迁移。线上 Compose 当时将：

```yaml
MIGRATE_DATABASE_ON_STARTUP: "false"
```

设置为关闭，导致容器启动后继续使用旧结构。

这与“没有管理员”是两个不同问题：一个是认证初始化状态不完整，另一个是数据库结构与代码版本不兼容。

### 4.5 发现 HTTPS 反向代理导致 exchange 同源校验失败

线上访问链路是：

```text
浏览器 HTTPS → 外层反向代理 TLS 终止 → PPT 容器内部 HTTP Nginx → FastAPI
```

原 PPT 容器 Nginx 在所有代理位置都使用：

```nginx
proxy_set_header X-Forwarded-Proto $scheme;
```

容器内部的 `$scheme` 是 `http`。因此 FastAPI 看到的协议是 HTTP，但浏览器发送的 `Origin` 是：

```text
https://ppt.cc2.cx
```

PPT 的 `/api/v1/auth/sso/exchange` 会校验 Origin、Fetch 元数据以及应用实际协议。当服务端错误地认为自己运行在 HTTP 下时，合法的 HTTPS 浏览器请求会被当作跨源请求，exchange 返回 `403`。

这解释了为什么“回调看起来成功，但最终仍然无法登录”：回调阶段已经写入交接 Cookie，真正建立应用会话的 exchange 阶段被协议判断拦截。

## 5. 根因汇总

| 根因 | 影响 | 典型表现 |
| --- | --- | --- |
| 接收端缺少 `SUB2API_SSO_SECRET` | 无法验证票据签名 | 回调跳转 `SSO_failed` |
| PPT 未完成管理员初始化 | SSO 用户不能成为首个管理员 | `configured=false`、用户数为 0 |
| PPT 数据库迁移落后 | 当前代码查询不存在的列 | `no such column: owner_id` |
| 内层 Nginx 覆盖 `X-Forwarded-Proto` | HTTPS 被识别为 HTTP | exchange 返回 `403` |

这些问题同时存在，导致线上环境即使修复其中一个问题，登录仍可能继续失败。

## 6. 修复方案与实施记录

### 6.1 统一 SSO 密钥

以 Sub2API 当前使用的密钥为基准，为 Canvas、PPT、QRCode 补齐同一个 `SUB2API_SSO_SECRET`，并重建相关容器。

处理原则：

- 不在代码、前端、日志或浏览器 URL 中写入密钥；
- 不生成多套“看起来相同但实际不同”的密钥；
- 重建后在容器内部检查长度和 SHA-256 指纹，而不是打印密钥内容；
- 其他服务的密钥也一并核对，避免后续切换应用时再次出现同类故障。

### 6.2 恢复 PPT 管理员认证元数据

在修改数据库前先创建带时间戳的备份。随后从线上已有迁移备份中仅恢复管理员认证所需的：

- `AUTH_USERNAME`；
- `AUTH_PASSWORD_HASH`。

没有覆盖线上已有的模型配置、API 配置、作品、上传文件或其他业务数据，避免把线上业务状态替换成本地状态。

### 6.3 完成 PPT 数据库迁移

将 PPT 数据库迁移到当前代码对应版本：

```text
e4c7a9b2d6f1
```

并把线上 PPT Compose 的启动迁移开关改为：

```yaml
MIGRATE_DATABASE_ON_STARTUP: "true"
```

之后强制重建 PPT 容器，使新镜像、新配置和迁移后的数据库同时生效。

### 6.4 修复反向代理协议传递

修改本地文件：

```text
ppt/nginx.conf
```

新增协议映射：

```nginx
map $http_x_forwarded_proto $presenton_forwarded_proto {
  ""      $scheme;
  default $http_x_forwarded_proto;
}
```

并将各代理位置改为：

```nginx
proxy_set_header X-Forwarded-Proto $presenton_forwarded_proto;
```

这样，当外层代理已经发送 `X-Forwarded-Proto: https` 时，容器不会把它覆盖成内部的 `http`；直接访问容器且没有该头时，仍然回退到 `$scheme`。

修改同步到线上后重新构建 PPT 镜像，并执行：

```text
nginx -t
```

检查通过。

## 7. 修复后的验证结果

### 7.1 线上容器和配置

复核结果：

- Sub2API、Canvas、PPT、QRCode、AIExcel、Ju、AICut、Screen2Code、Livart 容器均在运行；
- 所有相关容器的 SSO 密钥长度均为 64，指纹一致；
- PPT `/api/v1/auth/status` 返回 `configured=true`；
- PPT 数据库迁移版本为 `e4c7a9b2d6f1`；
- PPT 数据库有 2 个用户，其中 1 个管理员；
- `nginx -t` 返回 syntax ok / test is successful；
- 最近 30 分钟 PPT 日志未发现 `Traceback`、`OperationalError` 或启动失败错误。

### 7.2 PPT 新鲜票据端到端验证

使用重新生成的新票据（不使用旧 Cookie、不复用旧 ticket）验证：

```text
回调：HTTP/1.1 303 See Other
exchange：HTTP 200
authenticated：True
id_present：True
```

回调响应中的跳转为应用首页，交接 Cookie 具备 `HttpOnly`、`SameSite=Strict`、`Secure` 属性。验证结束后已删除临时 Cookie 测试目录。

### 7.3 Windows 本地测试

在 Windows 测试环境中显式设置了 Windows 可用的：

- `APP_DATA_DIRECTORY`；
- `TEMP_DIRECTORY`；
- `DATABASE_URL`。

运行 PPT SSO 集成测试：

```text
uv run pytest -q tests/integration/test_sub2api_sso.py
```

结果：

```text
30 passed
```

之前出现的 `/tmp/presenton`、`WinError 53` 是测试框架默认 Unix 临时路径造成的测试环境问题，不是 SSO 业务逻辑失败。

## 8. 以后部署前的检查清单

每次发布或迁移线上环境时，至少执行以下检查：

### 配置检查

- [ ] Sub2API 和每个接收应用都设置了同一个 `SUB2API_SSO_SECRET`；
- [ ] 不在日志中输出密钥原文，只输出长度和指纹前缀；
- [ ] 各应用的 SSO callback URL 与实际公网 HTTPS 地址完全一致；
- [ ] callback 路径不能被额外的查询参数、片段或错误前缀改变。

### 数据库检查

- [ ] 线上数据库已有至少一个管理员；
- [ ] 发布前执行数据库备份；
- [ ] 启动迁移开关与发布策略一致；
- [ ] 迁移版本与当前镜像代码匹配；
- [ ] 禁止用本地数据库覆盖线上业务数据，只迁移必要的认证元数据。

### 代理检查

- [ ] TLS 终止后保留 `X-Forwarded-Proto: https`；
- [ ] `Host`、`X-Forwarded-Host`、`X-Forwarded-For` 传递正确；
- [ ] exchange 请求的 Origin、Cookie、Fetch 元数据没有被代理清洗；
- [ ] 容器内 Nginx 不得无条件用 `$scheme` 覆盖外层协议。

### 发布后冒烟测试

- [ ] 使用新鲜 ticket 测试 callback；
- [ ] 使用新的 Cookie 测试 exchange；
- [ ] 确认 exchange 返回 200 且 `authenticated=true`；
- [ ] 确认刷新应用页面后会话仍然存在；
- [ ] 检查最近日志没有启动迁移错误、签名校验错误或同源校验错误。

## 9. 相关文件和部署改动

本地工作区的代码改动：

```text
C:\Users\kodesh\Desktop\CC2CX\ppt\nginx.conf
```

线上部署侧改动：

```text
/opt/cc2cxSub2api_260920/ops/docker-compose.production.yml
```

其中 PPT 的 `MIGRATE_DATABASE_ON_STARTUP` 已设为 `true`。数据库修改前已保留时间戳备份。

## 10. 安全与后续建议

本次排查使用的 root 凭据曾出现在聊天上下文中，虽然没有写入本日志，但仍建议立即轮换 root 密码，并优先改用 SSH 密钥登录、关闭密码登录或限制管理端口来源。

SSO 密钥也应存放在受保护的部署环境变量或密钥管理系统中，不应提交到 Git、写入前端构建产物或出现在浏览器请求 URL 中。

