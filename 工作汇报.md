# 工作汇报

**日期：** 2026-09-19  
**范围：** CC2CX 仓库 — Sub2API 中枢 + 8 个卫星 AI 应用  
**性质：** 架构落地、安全加固、模型约定、本地 Docker 部署  
**代码状态：** 均在工作区，尚未提交 Git（`main` 分支有大量已改 / 未跟踪文件）

---

## 一、今日要解决什么问题

目标不是“把八个项目拼进同一个进程”，而是做成 **一个门户、八个独立卫星**：

1. 用户只在 Sub2API 登录一次。
2. 点左侧菜单进入任意项目，立刻能用该用户自己的文本 / 图片 / 视频能力。
3. 卫星项目继续各自独立部署、独立进程、独立本地会话。
4. 计费和跨分组选上游账号只发生在 Sub2API 内部。
5. **SuperKey 永远不能当普通 API Key 用**，不能进浏览器、不能进 URL、不能进 SSO 票据。
6. 本地和云端只改两类环境变量：`LINK` 和 `{slug}_link`（以根目录 `本地链接.txt` 为准）。

今日工作把上述约束从方案推进到：**公共协议代码化、八个项目接入、SuperKey 防泄漏、公开模型目录、本地 Docker 全部拉起并 HTTP 验证。**

---

## 二、最终架构（已落地）

### 2.1 角色划分

| 角色 | 职责 |
| --- | --- |
| Sub2API（Go + Vue） | 登录、JWT、左侧菜单、SSO 发票据、`/v1` 网关、SuperKey、跨分组调度、计费 |
| 卫星应用 | 自己的业务数据、本地用户表、本地 Session；**不持有 SuperKey** |
| 浏览器 | 只拿卫星 Session；看不到 SuperKey、Sub2API JWT、上游 Key |

卫星保持独立进程，不并入 `sub2api/`。

### 2.2 用户点击菜单后的链路

```text
用户登录 Sub2API
  → 点左侧项目
  → GET /api/v1/auth/integrations/{slug}/start?next=/
  → 一次性 HMAC 票据（只含身份，不含 Key）
  → 卫星 /api/auth/sso/callback?ticket=...
  → 卫星建立本地用户 + Session
  → 浏览器之后只带卫星 Session
  → 卫星服务端调 Sub2API /v1：
        Authorization: Bearer <SUB2API_APP_CREDENTIAL>
        X-Sub2API-On-Behalf-Of: <Sub2API 用户 ID>
        X-Sub2API-Satellite: <slug>
  → 网关内部取出该用户 SuperKey，在全部分组里选上游（SuperKeyCrossGroup）
  → 用量记到该用户
```

PPT 的回调路径例外：`/api/v1/auth/sso/callback`（其余项目为 `/api/auth/sso/callback`）。

### 2.3 SSO 票据

- 格式：`base64url(JSON).base64url(HMAC-SHA256)`
- `iss=sub2api`
- 各项目 `aud`：canvas / ju / livart / presenton（ppt）/ openchatcut（aicut）/ screen2code / aiexcel / qrcode
- **不再附带 `rk`（加密 SuperKey / Relay Key）**
- 卫星若收到历史票据里残留的 `rk`，忽略，不解密、不使用

### 2.4 SuperKey 三条硬规则

1. 浏览器、SSO 票据、前端 Key 列表都看不到 SuperKey。
2. 把 `sk-super-` 当作 Bearer / `x-api-key` 打 `/v1` → **403 `SUPER_KEY_NOT_AN_API_KEY`**。
3. 卫星代调时，网关用应用凭证 + 用户 ID 识别身份，**在服务端内部**使用该用户 SuperKey 跨分组选号。

---

## 三、Sub2API 中枢改造

### 3.1 公共卫星层（避免每个项目复制一套 SSO）

新增目录 `sub2api/backend/pkg/satellite/`：

| 文件 | 作用 |
| --- | --- |
| `apps.go` | 八个卫星的 slug、audience、默认源站、回调路径、TTL |
| `headers.go` | 统一写三个请求头 |
| `env.go` | `LINK` / `{slug}_link` / 回调 URL 解析 |
| 配套 `*_test.go` | 目录与回调拼接测试 |

统一入口：

```http
GET /api/v1/auth/integrations/:slug/start
```

旧的 `canvas_sso.go`、`ju_sso.go`、`livart_sso.go` 等启动函数改为调用同一套 `startSatelliteSSO`，不再各写一套票据协议。新项目只需：

1. 在 `apps.go` 加一行；
2. 左侧菜单加一项。

新增 `satellite_sso.go`、`qrcode_sso.go`、`sso_super_key.go`。

### 3.2 网关：卫星代调 + SuperKey 拒绝当 Key

新增中间件 `super_key_satellite.go`（及测试）：

- `Authorization: Bearer <SUB2API_APP_CREDENTIAL>` 视为卫星应用凭证（不是用户 Key，也不是 SuperKey）。
- 必须同时带合法的 `X-Sub2API-On-Behalf-Of`（Sub2API 用户数字 ID）和 `X-Sub2API-Satellite`。
- 凭证比对使用 HMAC 恒定时间比较。
- 呈现 `sk-super-` 前缀的凭证一律拒绝。

调度侧：`SuperKeyCrossGroup` 在用户所有活跃分组里选上游账号（含混合平台），不再把卫星流量钉死在单一分组；Gemini 兼容路径仅在请求显式带 `X-Sub2API-Group-ID` 时才钉分组。

### 3.3 SuperKey 防泄漏（控制台 / 仓储 / DTO）

| 点 | 行为 |
| --- | --- |
| 按用户列出 Key | 排除名为 Super 以及 `sk-super-` 前缀 |
| DTO `APIKeyFromService` | SuperKey 的 `Key` 字段置空 |
| `GetByID` | SuperKey 返回 404 |
| 更新 | 禁止改 SuperKey |
| 创建 | 已拒绝 `sk-super-` 前缀 |
| 管理端列表 | 走同一套仓储查询，不会把 SuperKey 当普通 Key 漏出 |

已删除 / 停用：向票据附加加密 SuperKey、`encryptRelayKey`、ju 票据 `rk`、canvas 解密 Relay Key 等路径。

### 3.4 前端菜单

`AppSidebar.vue` 按 `{slug}_link` 跳转八个项目；配套单测已改。本地默认：

| 环境变量 | 本地地址 |
| --- | --- |
| `aicut_link` | http://localhost:5199 |
| `aiexcel_link` | http://localhost:4173 |
| `canvas_link` | http://localhost:3522 |
| `ju_link` | http://localhost:3000 |
| `livart_link` | http://localhost:8080 |
| `ppt_link` | http://localhost:8341 |
| `qrcode_link` | http://localhost:5221 |
| `screen2code_link` | http://localhost:5173 |

云端对应 `https://{slug}.cc2.cx`。模型统一入口本地 `LINK=localhost:18080`，云端 `https://api.cc2.cx`。

### 3.5 公开模型目录（代码）

新增 `sub2api/backend/internal/domain/satellite_models.go`：

- 默认文本 `gpt-5.5`
- 默认图片 `gpt-image-2`
- 默认视频 `grok-imagine-video-1.5`
- 以及已对接项目实际在用的兼容名（见第六节）

卫星只许发这份目录里的公开名，不在卫星进程里发明第三套模型字符串。

---

## 四、八个卫星项目分别做了什么

原则一致：**SSO 只建立身份；模型调用走 Sub2API `/v1` + 三个头；无当前用户则 401，禁止回退共享 `.env` Key。**

### 4.1 canvas（无限画布，端口 3522）

- SSO 身份票据，忽略历史 `rk`。
- 中继地址：`SUB2API_RELAY_BASE_URL` 优先，否则回退 `LINK`。
- 预设：文本 `gpt-5.5`；图片 `gpt-image-2,grok-imagine-image-1.5`；视频 `grok-imagine-video-1.5,seedance-2.0,kling-v3`。
- 随 Sub2API compose override 一起部署：`canvas-app`，宿主机 `127.0.0.1:3522→3000`，容器内 `LINK=http://sub2api:8080`。

### 4.2 ju（智能剧场，端口 3000）

- 票据不再带 `rk`；本地用户用 `(provider=sub2api, subject)`。
- 已移除静态 `SUB2API_RELAY_API_KEY`；运行时只使用服务端 `SUB2API_APP_CREDENTIAL` 并绑定当前 Session subject。
- 视频向公开名靠齐：`grok-imagine-video-1.5`、`seedance-2.0`、`kling-v3`（旧镜像不认识这些名字，今日已用当前源码重建 backend 镜像）。
- compose：`docker-compose.local.yml` + `docker-compose.sub2api.yml`，加入 `deploy_sub2api-network`，nginx 仍访问主机名 `backend`。
- 建议 `CANVAS_AUTO_MIGRATE=true`（本地 sqlite）。

### 4.3 livart（对话 + 生图，端口 8080）

- Java SSO 改为身份票据；新增 `Sub2ApiRelayKeys.java`。
- 默认聊天 `gpt-5.5`，默认生图 `gpt-image-2`；产品没有视频工作台，不硬造视频入口。
- 测试：`AuthServiceSsoTest`、`Sub2ApiSsoTicketsTest`、`UserApiConfigServiceTest` 已按新协议调整。
- Docker：Postgres + RabbitMQ + MinIO + 应用。今日为打通 Flyway，**本地 Postgres 数据被重置**（见第八节）。

### 4.4 ppt / Presenton（端口 8341）

- 回调：`/api/v1/auth/sso/callback`，`aud=presenton`。
- 新增 `utils/sub2api_satellite.py`：从 `LINK` 推导 `{origin}/v1`，httpx 钩子自动加三个头。
- 默认文本 `gpt-5.5`，图片 `gpt-image-2`。
- 生产 compose **只起 `production`**，不起 GPU / development（避免抢 1455 端口和误启 Ollama）。

### 4.5 aicut / OpenChatCut（端口 5199）

- 新增 `server/gateway/satellite.ts`：应用凭证 + on-behalf-of；本地 UI 名映射到公开模型名（如 grok-imagine → `grok-imagine-image-1.5`，kling → `kling-v3`）。
- SSO 校验改为身份票据；历史 SuperKey 解密断言已改掉。
- 网关开启后，门户流量不再用厂商 Key。

### 4.6 aiexcel（端口 4173）

- 只有文本 Copilot，模型 `gpt-5.5`。
- SSO 忽略残留 `rk`（以前解密失败会直接导致登录失败）。
- 容器 `4173→80`。

### 4.7 qrcode（端口 5221）

- 新增 `src/sso.js`，回调 `/api/auth/sso/callback`。
- 艺术二维码走 `/v1/images/edits`，模型 `gpt-image-2`。
- 修正 compose 里重复的 `QR_SESSION_SECRET` 键（YAML 无法解析）。

### 4.8 screen2code（端口 5173，后端 7001）

- 认证路由与模型选择测试按门户协议改写。
- 文本默认 `gpt-5.5`（thinking 档仍用这个公开名，思考参数走请求体）。
- 门户模式下视频输入也走同一会话，不再要 Gemini Key。
- 新增 `.env.example`。

---

## 五、环境变量约定（本地 / 云端只改值）

对照文件：`本地链接.txt`、`docs/菜单链接环境配置.md`。

### 5.1 给浏览器看的（宿主机）

```env
LINK=localhost:18080
aicut_link=http://localhost:5199
aiexcel_link=http://localhost:4173
canvas_link=http://localhost:3522
ju_link=http://localhost:3000
livart_link=http://localhost:8080
ppt_link=http://localhost:8341
qrcode_link=http://localhost:5221
screen2code_link=http://localhost:5173
```

云端：`LINK=https://api.cc2.cx`，`{slug}_link=https://{slug}.cc2.cx`。

### 5.2 给容器看的（Docker DNS）

卫星容器不能把 `LINK=localhost:18080` 原样灌进去（那会打到容器自己）。覆盖文件：

`sub2api/deploy/.env.satellites`

```env
LINK=http://sub2api:8080
SUB2API_RELAY_BASE_URL=http://sub2api:8080/v1
```

SSO 回调必须仍是浏览器能打开的 `http://localhost:<端口>/api/auth/sso/callback`（PPT 为 `/api/v1/auth/sso/callback`）。

其它必需项：`SUB2API_SSO_SECRET`、`SUB2API_APP_CREDENTIAL`。网络名：`deploy_sub2api-network`。

---

## 六、公开模型（卫星实际发出去的名字）

文档：`docs/卫星公开模型.md`。

| 能力 | 接口 | 默认公开名 |
| --- | --- | --- |
| 文本 | `POST {LINK}/v1/chat/completions` 或 `/v1/responses` | `gpt-5.5` |
| 图片 | `POST {LINK}/v1/images/generations`、`/v1/images/edits` | `gpt-image-2` |
| 视频 | `POST {LINK}/v1/videos`，轮询 `GET {LINK}/v1/videos/{id}` | `grok-imagine-video-1.5` |

各项目预设（不是全量目录）：

| slug | 文本 | 图片 | 视频 |
| --- | --- | --- | --- |
| canvas | gpt-5.5 | gpt-image-2、grok-imagine-image-1.5 | grok-imagine-video-1.5、seedance-2.0、kling-v3 |
| ju | gpt-5.5 | gpt-image-2 | grok-imagine-video-1.5、seedance-2.0、kling-v3 |
| livart | gpt-5.5（仍允许 gpt-5.4-mini） | gpt-image-2 | — |
| ppt | gpt-5.5 | gpt-image-2 | — |
| aicut | 目录文本 + 本地名映射 | 映射后的公开图 | 映射后的公开视频 |
| screen2code | gpt-5.5 | 同一 OpenAI 会话 | 门户模式不再要 Gemini |
| aiexcel | gpt-5.5 | — | — |
| qrcode | — | gpt-image-2（edits） | — |

没有该能力的产品不硬造入口。UI 可以保留厂商选项，出站必须换成公开名。

---

## 七、文档与协作约定

今日整理 / 新增，给后续人和 AI 用，避免再发明协议：

| 文档 | 用途 |
| --- | --- |
| `docs/卫星应用接入公共层.md` | 接入清单：SSO、三个头、SuperKey、多用户、环境变量 |
| `docs/卫星公开模型.md` | 公开名、默认值、项目对照、本地名映射 |
| `docs/菜单链接环境配置.md` | `LINK` / `{slug}_link` 本地与云端 |
| `AGENTS.md` | 仓库入口：先读公共层，再改卫星 |
| `sub2api/skills/satellite-integrate/SKILL.md` | 新项目接入技能 |
| `本地链接.txt` | 唯一的地址对照表 |

根目录 README、SSO 设计方案也按身份票据 + 代调协议做了同步说明。

---

## 八、本地 Docker 部署（今日后半段）

用户要求：协议完善后，**按链接文档把项目都部署进 Docker**（本地，不是云端 `api.cc2.cx`）。

### 8.1 部署方式

- 中枢：`sub2api/deploy`（含 canvas override）
- 脚本：`sub2api/deploy/up-satellites.ps1`  
  先起 Sub2API + canvas，再逐个起卫星；ppt 只起 `production`
- 环境：`--env-file deploy/.env` + `--env-file deploy/.env.satellites`

### 8.2 部署过程中实际遇到并处理的问题

1. **Docker Desktop 多次未就绪 / 中途崩溃**  
   引擎管道 `dockerDesktopLinuxEngine` 会消失。处理：重启 Docker Desktop，等 `docker ps` 真正可用后再继续。并行 `--build` 时更容易把引擎打挂，改为串行。

2. **卫星误用宿主机 `LINK=localhost:18080`**  
   容器内 localhost 不是中枢。处理：`.env.satellites` 覆盖为 `http://sub2api:8080`。

3. **qrcode compose YAML 非法**  
   `QR_SESSION_SECRET` 写了两次。已删重复键。

4. **ju 旧镜像不认公开视频名**  
   报 `grok-imagine-video-1.5` unsupported。已用当前源码重建 `open-ai-canvas-backend:local`，backend 恢复 healthy。

5. **livart Flyway / 库连错**  
   - 把中枢 `.env` 的 `POSTGRES_USER/DB/PASSWORD` 传给 livart，库名和账号被污染。  
   - livart 与 Sub2API 同网后，主机名 `postgres` 解析到**中枢库**，出现 `password authentication failed for user "postgres"`。  
   - 处理：卫星 env 覆盖 livart 自己的 Postgres 账号；Postgres 容器改名为 `livart-postgres`，`DB_HOST=livart-postgres`。  
   - 为跑通全新迁移，**本地 livart Postgres 卷已清空**，Flyway 成功应用到 v13。  
   - livart 崩溃循环（不断映射 8080）会拖垮 Docker Desktop；修库之前先 `restart=no` 停循环。

6. **ppt** 使用已有 `ppt:sub2api-integration` 镜像起 `production`，避免再打一次重镜像把引擎打挂。

7. **aicut** 完整重建曾因 buildx EOF 失败；当前运行的是已有 `aicut:workspace` 镜像，容器内 `LINK` 已是 `http://sub2api:8080`，端口 5199 可访问。如需最新 `satellite.ts` 进镜像，Docker 稳定后应再 `--build` 一次。

### 8.3 验证结果（引擎稳定窗口内）

全部返回 HTTP 200：

| 服务 | 地址 | 结果 |
| --- | --- | --- |
| Sub2API | http://localhost:18080 | 200 |
| canvas | http://localhost:3522 | 200 |
| ju | http://localhost:3000 | 200 |
| livart | http://localhost:8080/api/health | 200 |
| aicut | http://localhost:5199 | 200 |
| aiexcel | http://localhost:4173 | 200 |
| ppt | http://localhost:8341 | 200 |
| qrcode | http://localhost:5221 | 200 |
| screen2code | http://localhost:5173 | 200 |

当时相关容器均为 Up（ju-backend / livart / sub2api 为 healthy）。

本机 Docker Desktop 仍会偶发退出。容器多为 `unless-stopped`，引擎起来后应自动恢复。若 `docker ps` 连不上，先打开 Docker Desktop。

---

## 九、测试与未在本机跑通的部分

已覆盖方向：

- Sub2API：SSO / SuperKey / 卫星中间件相关 Go 测试（需在 `sub2api/backend` 下跑，根目录没有 Go module）
- livart：SSO / 票据 / 用户 API 配置单测（改过源码；本机未装 Maven，今日未重跑）
- ppt：`test_sub2api_satellite.py`
- screen2code：`test_model_selection.py`
- 前端：`AppSidebar.spec.ts`

环境限制（预先存在，不是今日回归失败）：

- 根目录 `go test` 无 module
- canvas 在 Windows 上 sqlite 临时目录锁
- ju 部分测试依赖 CGO sqlite
- 默认 Python 无 pytest
- livart 无本机 `mvn`

因此“协议正确性”主要靠定向单测 + Docker HTTP 探活，**尚未做完整的登录 → 点菜单 → 真实出图/出视频的端到端人工验收。**

---

## 十、风险、未完成项、建议下一步

1. **Git 未提交。** 今日全部改动仍在工作区。建议按“公共层 / 各卫星 / 部署脚本”分批提交，不要把 `deploy/.env` 里的密钥提交进库（`.env.example` 可以）。
2. **Docker Desktop 不稳定。** 并行构建、livart 崩溃循环都会让引擎掉线。后续部署用 `up-satellites.ps1` 串行；先保证 livart 不要 `Restarting`。
3. **aicut 镜像可能不是最新源码。** 端口已通，但完整 `--build` 曾失败。
4. **livart 本地库已重置。** 只影响本机 Docker 卷，不是云端数据。
5. **端到端 SSO 未在浏览器里点通。** 建议登录 Sub2API 后逐个点左侧八个菜单，确认回调、本地 Session、以及一次真实文本/图片请求。
6. **本次是本地 Docker，不是云端 HTTPS。** 上线时只改 `LINK=https://api.cc2.cx` 和八个 `https://{slug}.cc2.cx`，SSO 回调改成对应域名。
7. **知识库向量。** livart 启动日志提示缺少 `LIVART_KNOWLEDGE_EMBEDDING_API_KEY`，不影响健康检查，影响知识索引。
8. **新项目接入路径已写死：** 读 `docs/卫星应用接入公共层.md` → `apps.go` 加一行 → 菜单加一项 → 三个头调 `/v1`。不要再复制一套 SSO Handler。

---

## 十一、主要改动文件（按模块）

未提交。下列为今日工作区中与本次任务直接相关的部分。

### Sub2API

- 新增：`backend/pkg/satellite/`、`internal/domain/satellite_models.go`、`internal/handler/satellite_sso.go`、`qrcode_sso.go`、`sso_super_key.go`、`middleware/super_key_satellite.go` 及测试
- 修改：各 `*_sso.go`、API Key 仓储/服务/DTO、网关调度、Gemini 兼容、`AppSidebar.vue`、各 docker-compose、`deploy/.env.example`
- 新增部署：`deploy/.env.satellites`、`deploy/up-satellites.ps1`
- 新增技能：`skills/satellite-integrate/SKILL.md`

### 卫星

- canvas / ju / livart / ppt / aicut / aiexcel / qrcode / screen2code：SSO、中继、模型预设、compose 入网
- 新增：`aicut/server/gateway/satellite.ts`、`ppt/.../sub2api_satellite.py`、`qrcode/src/sso.js`、`livart/.../Sub2ApiRelayKeys.java`

### 文档

- 新增：`docs/卫星应用接入公共层.md`、`docs/卫星公开模型.md`、`AGENTS.md`
- 修改：`本地链接.txt`、`docs/菜单链接环境配置.md`、`docs/README.md`、根 README、SSO 设计方案

---

## 十二、一句话结论

今天把「Sub2API 当门户、八个项目当卫星」从约定做成了可运行的本地栈：身份 SSO、隐藏 SuperKey、公开模型名、`LINK` / `{slug}_link` 两套地址，并按 `本地链接.txt` 的本地端口全部进了 Docker，九个入口 HTTP 200。剩余主要是：**提交代码、Docker 引擎稳定性、浏览器点菜单的端到端验收、以及上线时只替换云端 `LINK` 和域名。**
