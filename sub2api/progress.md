# sub2api 构建进度

## 2026-08-31

- 已确认任务边界：仅构建和运行 `sub2api`。
- 已检查工作区顶层结构和关键构建文件。
- 已开始阶段 1：检查仓库与运行环境。
- 阶段 1 完成：构建工具满足要求；项目是无 `.git` 元数据的源码快照。
- 已选定 `deploy/docker-compose.dev.yml` 作为源码构建和运行入口。
- 阶段 2 进行中：检查端口、Docker 资源和本地配置。
- 发现旧 Sub2API 健康运行于 8080；决定当前源码部署改用 18080，并使用独立 Compose 项目名。
- 阶段 2 完成：已创建受忽略的 `deploy/.env`，Compose 配置校验通过。
- 阶段 3 开始：从当前源码构建应用镜像。
- 阶段 3 完成：前端、Go 后端和最终 Docker 镜像均构建成功。
- 阶段 4 开始：启动容器并进行健康验证。
- 三个容器已启动并初步健康，健康接口返回 200/ok。
- 最终核验尝试 1 因 Docker 模板引号被 PowerShell错误处理而中止；已定位根因，准备改用 JSON 解析。
- JSON 标签读取方式已通过最小复现验证。
- 最终核验尝试 2 通过运行与 HTTP 基础检查，但登录响应字段断言失败；正在诊断响应封装。
- 已确认登录响应封装并验证管理员登录；PostgreSQL 接受连接，Redis 返回 PONG。
- 单体最终核验后续触发 PowerShell `$HOME` 变量冲突；决定拆分为配置/容器、HTTP/登录、依赖、旧服务四项独立核验。
