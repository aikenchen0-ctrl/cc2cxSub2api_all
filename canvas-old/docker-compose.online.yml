services:
  canvas:
    build:
      context: .
      dockerfile: Dockerfile.online
    image: sub2api/canvas:online-http
    container_name: infinite-canvas
    restart: unless-stopped
    ports:
      - "${CANVAS_PORT:-3522}:3000"
