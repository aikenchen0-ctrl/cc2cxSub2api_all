# Screen2Code 国际化设计

## 目标

为 Screen2Code 前端增加英文与简体中文可选语言，支持浏览器语言初始识别、用户手动切换和本地持久化，不改变模型请求、代码生成结果、文件内容或后端接口。

## 范围

首轮覆盖普通用户流程：开始页、文本/截图/上传/导入、设置、设计系统、预览、历史记录、Agent 活动、提示框、错误和状态提示。`/evals` 内部评测页面保留英文，后续单独处理。

## 方案

- 在 `frontend/src/i18n/` 新增语言类型、翻译资源、翻译上下文和 `useI18n` 钩子。
- 语言资源使用稳定键名，缺少翻译时回退英文，避免页面出现空文本。
- 语言选择保存在 `localStorage` 的独立键中；无保存值时按 `navigator.language` 选择中文或英文。
- 设置页增加语言下拉框，切换后通过 React 状态让所有使用翻译钩子的组件即时重绘。
- 组件只翻译产品文案；模型名、技术名、代码、用户输入、文件名和动态变量保持原样。
- 不引入第三方国际化包，减少构建和运行时依赖。

## 验证

- `pnpm exec tsc --noEmit`
- `pnpm run build`
- 现有前端测试
- 检查英文默认、中文切换、刷新后语言保持、缺失键回退和动态错误消息。

## 发布

从 `origin/main` 创建 `feat/screen2code-i18n` 分支，提交设计和实现，推送该分支到 GitHub；不直接改写 `main`，不触碰当前生产工作区。
