# 归因算法工作报告

> 工作范围：本轮完成独立卫星应用 `modelaudit/`，并接入现有 Sub2API。  
> 报告日期：2026-09-26  
> 当前状态：代码、离线校准和接入验证已完成；真实上游扫描尚未执行。

## 1. 目标与边界

本轮的目标是建立一个独立的上游审计面板，按可配置间隔（默认 60 分钟，仅允许 30 或 60 分钟）扫描 Sub2API 中的 active 上游账号。每个账号分别记录四类结果：

1. **ModelTrace 模型指纹归因**：判断实际返回是否仍与配置的期望型号一致。
2. **隐藏 input token 检查**：用短请求比较本地 token 估算和网关回包 usage。
3. **缓存写入/读取检查**：验证同一长前缀在第二次请求是否产生 cache read。
4. **倍率筛查**：在供应商提供可核验计数器时，比较上游增量、账号配置倍率和预期供应商倍率。

应用只通过 Sub2API 已有的 HTTP 接口工作，不改造上游协议，也不引入新的 SSO、API Key 传递或菜单跳转协议。账号列表、详情和 usage 只读；不调用账号导出接口，不调用 `POST /api/v1/admin/accounts/:id/test`，不把上游 token 写入报告或日志。

本报告中的“归因”是带门槛的筛查结果。闭集指纹库之外的真实新型号、实际扣费金额和真实降档行为，都不能仅凭本轮离线结果宣称已验证。

## 2. 端到端工作流程

### 2.1 读取账号和配置

- 管理端使用 `SUB2API_ADMIN_API_KEY`，没有该值时才使用 `SUB2API_ADMIN_JWT`。
- 只读取：
  - `GET /api/v1/admin/accounts?status=active`
  - `GET /api/v1/admin/accounts/:id`
  - `GET /api/v1/admin/accounts/:id/usage`
- 账号详情在内存中先清洗，只保存账号 ID、名称、平台、类型、状态、`schedulable`、并发、倍率、分组等安全字段。
- `active` 但 `schedulable=false` 的账号记为 `skipped`，不发送探针。
- 每次扫描前从环境变量读取分组对应的普通网关 API Key 和探针档案；配置校验会拒绝 `sk-super-*`，SuperKey 不会作为普通 Key 使用，也不会进入浏览器。

### 2.2 账号钉号和请求路由

Sub2API 的稳定 session 只能让后续请求粘到首次选中的账号，不能指定首次选中哪一个账号。因此先统计每个分组内 active 且 schedulable 的账号数量，只使用“恰好一个账号”的独占分组。

对每个候选账号：

1. 用账号 ID 和本地 session secret 派生稳定的 `X-Session-Id`。
2. 同时写入 Sub2API 现有格式的 `metadata.user_id`，其中设备标识也由本地 secret 派生。
3. 通过普通网关 Key 发一次 `Say hi` 校准请求，确认请求确实命中目标账号。
4. 校准失败、共享分组或没有独占分组时，四项结果标记为 `unpinned`，相关证据不参与模型或费用归因。
5. 同一账号的所有探针复用同一个 session；账号之间用全局 semaphore 限制并发，单个账号内按顺序执行。

Anthropic OAuth/setup-token 账号使用原生 `/v1/messages` 路由、固定的 `claude-cli/2.1.161 (modelaudit probe)` User-Agent 和稳定 metadata，避免触发 Claude Code system prompt 注入。其他账号使用 `/v1/chat/completions`。

### 2.3 探针执行顺序和存储

钉号成功后，先读一次账号 usage，执行 token delta 与缓存探针，再读一次 usage，随后执行倍率筛查和 ModelTrace。每个探针独立保存状态、严重级别、请求 ID、数值指标和原因；原始 prompt、原始响应、管理 Key、普通 Key、账号凭据不落库。

结果写入 SQLite（默认 `data/modelaudit.sqlite3`），每轮另写 `data/reports/<run-id>.json`。报告包含账号级结果和运行统计，不包含上游秘密或原始对话内容。

## 3. ModelTrace 归因算法

### 3.1 推理过程

- 复用本地 `ModelTrace/fingerprint.py`、`unified_bank.json` 和其闭集指纹分析逻辑；容器中使用 `modelaudit/vendor/modeltrace/` 副本作为回退。
- 每次生成三条长整数挑战，发送给目标上游并解析返回的整数序列；不运行 ModelTrace 网页、不启用 Codex 插件，也不附带 Claude Code system prompt。
- 对三条结果做全局特征分析，得到候选型号、候选概率、候选 profile similarity 和与第二候选的概率 margin。
- 指纹库是闭集。证据不达门槛、校准文件不可用、哈希不匹配、回答截断或挑战未完整返回时，结果为 `unknown`；不会把未知结果硬归因到某个具体型号。

### 3.2 判定门槛

当前校准文件 `modelaudit/vendor/modeltrace/candidate_thresholds.json` 的门槛为：

- 顶部候选概率：`>= 0.95`；
- 顶部候选与第二候选的 margin：`>= 0.20`；
- 候选型号专属的 similarity 下限：由省略该型号后的代理样本最大相似度再加固定 `0.10` 得出；运行时还可设置更高的全局下限。

校准文件绑定以下 SHA-256，任一源码或指纹库改变后必须重新生成校准：

- `unified_bank.json`: `1c2cb74d372f9f0f30d0dabbb7b7a838660d2f769a88d0c8489e4c662e088c21`
- `fingerprint.py`: `387fe5c44cc5f859995432267b0d3b462fc26e82419fc22e3ee7f36cc2502d98`

期望型号在指纹库外、校准文件缺失或运行时门槛低于校准基线时，直接返回 `unknown`。高置信且与期望型号不同才会产生红色 ModelTrace 告警；候选名称在低置信 unknown 结果中隐藏。

### 3.3 离线校准结果

使用 `modelaudit/tools/evaluate_modeltrace_thresholds.py` 做四个环境轮转的训练、校准、验证隔离。结果如下：

| 情形 | 结果 |
| --- | ---: |
| 已知样本首选候选正确 | 186 / 192 |
| 已知样本同时达到当前识别门槛 | 11 / 192 |
| 移除参考型号得到的库外代理样本误通过 | 0 / 192 |
| 将候选 similarity margin 从 `0.10` 放宽到 `0.075` 后，已知样本通过 | 19 / 192 |
| 同样放宽后的代理样本误通过 | 2 / 192 |

“移除参考型号”只是代理未知样本，不能代表真实新型号的开放集准确率或误报率。因此生产配置保留 `0.10`，并把该限制写入校准文件和报告。

ModelTrace 的成本只按本地 tokenizer、返回文本和配置的 LiteLLM/手工价格表估算；不读取 ModelTrace 回包的 `usage`，也不把这部分估算混入倍率账单对账。若某挑战未完整返回，成本状态会标为部分估算。

## 4. 其他三类检查

### 4.1 隐藏 input token

发送无 system prompt 的 `Say hi`，用 `tiktoken`（不可用时使用明确的 fallback）估算期望输入 token，计算：

```text
delta = expected_input_tokens - reported_input_tokens
```

绝对 delta `<=20` 视为正常误差，`>20` 为黄色信号，`>100` 为红色信号。它是筛查信号，不单独决定模型归因或自动停用。

OpenAI usage 中的 `prompt_tokens` 已包含 cached tokens，代码只做一次 `prompt_tokens - cached_tokens`；Anthropic 的 `input_tokens`、`cache_creation_input_tokens`、`cache_read_input_tokens` 分开处理，不套用 OpenAI 的减法。

### 4.2 缓存写入/读取

按分组的 `cache_min_prefix_tokens` 生成超过阈值的长前缀，保证两次请求的序列化请求体完全一致，并在同一已钉住的 session 中重放。第二次回包的 cache read token 大于零才标记 `ok`；无法生成有效前缀或无法解析 usage 时标记为未定，第二次仍为零时标记红色缓存告警。

OpenAI 的缓存读取来自 `prompt_tokens_details.cached_tokens`；Anthropic 使用 `cache_creation_input_tokens` 和 `cache_read_input_tokens`。价格按固定 LiteLLM 快照或分组手工价格估算，但当前允许读取的 Sub2API 账号接口不提供逐请求网关扣费，所以缓存费用核对保持 `inconclusive`，不凭估算声称已验证实际扣费。

### 4.3 隐藏倍率

当账号 usage 同时提供有效的供应商计数器时：

- token 口径：`供应商 token 配额增量 / 已钉号响应中的 input、output、cache token 总量`；
- 账单口径：`供应商账单金额增量 / 本地价格估算的探针成本`。

两种口径分开记录，再分别与账号 `rate_multiplier`、配置的 `expected_provider_cost_multiplier` 和名义 `1.0` 比较。默认“相差一个数量级”（10 倍或 1/10）才产生倍率不匹配信号。计数器缺失、计费窗口改变、增量不为正或可能混入并发流量时，结果为 `inconclusive`，不伪造单次探针账单。

ModelTrace 和钉号校准的成本会单独写入估算字段；usage-aware 倍率总额只包含 token delta 与缓存探针，避免把两种成本口径混成一个“智能分数”。

## 5. 告警和自动停用

自动停用默认关闭，面板必须显式打开总开关并选择 `modeltrace_red` 规则。唯一允许触发停用的情况是：第一轮已经得到高置信、红色且与期望型号不同的 ModelTrace 结果，第二轮独立三挑战再次得到同一候选型号的高置信红色结果。

确认成功后调用既有 `POST /api/v1/admin/accounts/:id/schedulable` 将账号设为不可调度，并写入审计记录；不会自动恢复账号。未确认的红色结果只告警，不停用。共享分组、未钉号、unknown、token delta 或倍率告警均不具备自动停用资格。

## 6. Sub2API 接入

- 后端在 `sub2api/backend/pkg/satellite/apps.go` 注册 `modelaudit`，使用现有 `GET /api/v1/auth/integrations/:slug/start` SSO 启动接口。
- 左侧菜单新增 ModelAudit SSO 项，`includeApiKey: false`，浏览器不接收 SuperKey 或普通网关 Key。
- 本地链接配置为 `http://localhost:8077`；线上链接为 `https://modelaudit.cc2.cx`，线上回调为 `/api/auth/sso/callback`。
- Docker 服务只绑定宿主机 `127.0.0.1:8077`，加入 Sub2API 网络，健康检查为 `/health`。
- SSO ticket 只提供身份，后续管理会话仍通过既有用户身份接口校验；角色权限不从 ticket 中臆造。
- 若前面有 Nginx，必须开启 `underscores_in_headers on`，否则 `X-Session-Id` 可能在转发时丢失。

## 7. 已完成的验证

- Python 测试：`50 passed`。
- Go `pkg/satellite` 测试通过。
- 侧栏 Vitest：`17 passed`。
- Docker 镜像构建通过。
- 容器内 tokenizer 可用；校准文件状态为 `verified`，包含 16 个候选型号。
- 本地 Sub2API health 返回 HTTP 200。

## 8. 尚未验证和上线前条件

本轮工作区没有 `modelaudit/.env`，部署配置也没有可用的 Sub2API 管理 API Key/JWT、普通网关 Key 和分组档案。因此本轮没有发起真实上游探针，以下结论仍待真实环境验证：

- 实际上游账号的型号降档检测；
- 真实未知型号的开放集拒识能力；
- 供应商真实扣费倍率和缓存计费方式；
- 自动停用调用在生产权限和路由配置下的实际结果。

上线时只需在不提交仓库的 `.env` 中补齐管理凭据、普通网关 Key、分组模型/价格/缓存档案和 SSO 配置，再先运行一次手动扫描检查钉号结果。密钥不应写入报告、SQLite、日志或浏览器。

## 9. 主要实现文件

- 需求与约束：[`提示词.txt`](提示词.txt)
- 使用说明和部署流程：[`modelaudit/README.md`](modelaudit/README.md)
- 调度、钉号和自动停用：[`modelaudit/app/scheduler.py`](modelaudit/app/scheduler.py)
- ModelTrace 归因：[`modelaudit/app/probes/modeltrace.py`](modelaudit/app/probes/modeltrace.py)
- 校准加载与哈希校验：[`modelaudit/app/probes/trace_calibration.py`](modelaudit/app/probes/trace_calibration.py)
- 离线阈值评估：[`modelaudit/tools/evaluate_modeltrace_thresholds.py`](modelaudit/tools/evaluate_modeltrace_thresholds.py)
- 候选阈值文件：[`modelaudit/vendor/modeltrace/candidate_thresholds.json`](modelaudit/vendor/modeltrace/candidate_thresholds.json)
- Sub2API 注册与菜单测试：[`sub2api/backend/pkg/satellite/apps_test.go`](sub2api/backend/pkg/satellite/apps_test.go)、[`sub2api/frontend/src/components/layout/__tests__/AppSidebar.spec.ts`](sub2api/frontend/src/components/layout/__tests__/AppSidebar.spec.ts)
