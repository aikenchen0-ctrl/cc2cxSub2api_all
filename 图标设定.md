# 图标设定

## 卫星应用

所有卫星应用的 favicon 默认读取 Sub2API 的公开设置接口：

```text
GET /api/v1/settings/public
```

卫星应用读取返回值中的 `data.site_logo`，并将其设置为页面 favicon。`site_logo` 可以是 Sub2API 设置中配置的相对路径、绝对 URL 或数据 URL。为保证未配置自定义 Logo 时仍使用统一品牌，空值时回退到 Sub2API 的 `/logo.jpg`。

本地 Docker 环境通过 `http://sub2api:8080` 访问 Sub2API；浏览器端根据访问环境使用本地 Sub2API 地址或线上地址，过程中不携带 SuperKey、JWT、Authorization 或用户 API Key。

## AgentAPI 与分站

AgentAPI 支持独立设置自己的 Icon。AgentAPI 创建的分站中，卫星应用的图标应同步使用该分站设置的 Logo，而不是继续使用主站 Logo。

> 当前状态：AgentAPI 独立 Icon 的设置约定已保留，但“分站 Logo 自动同步到分站内卫星应用”的能力尚未实现。实现该能力时，应继续复用公开设置和现有 SSO/公共层协议，不在浏览器中传递 SuperKey 或其他服务端凭证。
