# 卫星计费配置

## 1. 功能概览

Sub2API 管理端新增“卫星计费”页面，用于分别配置卫星应用的计费方式和价格规则。入口为管理员侧栏中的“卫星计费”，路由为：

```text
/admin/satellite-billing
```

页面以标签页切换卫星应用。每个应用拥有独立配置；`livart` 目录下的相关功能共用 `livart` 这一份配置。

当前支持的应用：

```text
aicut, aiexcel, ai3d, aihuoke, canvas, ju,
livart, ppt, qrcode, screen2code, yibiao
```

## 2. 计费方式

每个卫星应用只能选择一种计费方式：

| 模式 | 说明 |
| --- | --- |
| `model` | 沿用 Sub2API 当前模型、渠道和分组定价。没有独立规则时也保持该行为。 |
| `custom` | 使用卫星自己的规则，可按模型或 API 路径配置价格。支持每百万 Token、每张图片、每秒视频和每次任务。 |
| `request` | 按任务、图片数量或视频时长收费。 |
| `points` | 按规则扣除积分；积分按 `100 积分 = 1 美元` 折算。 |

规则字段如下：

```json
{
  "target": "gpt-image-1",
  "unit": "image",
  "amount": 0.25
}
```

- `target`：模型名、API 路径或 `*` 通配符。
- `unit`：`millionTokens`、`image`、`videoSecond` 或 `request`。
- `amount`：美元单价，或积分模式下的积分数。

## 3. 规则匹配和扣费

计费时按以下顺序匹配：

1. 请求 API 路径（会移除查询参数和末尾 `/`）。
2. 请求模型名。
3. `*` 通配规则。

在同一匹配目标下，系统根据请求实际使用的单位选择规则。未命中卫星规则时，继续使用原有模型计价。

独立卫星价格只覆盖用户的 `ActualCost` 和计费模式，不叠加模型、分组、峰值或图片倍率；模型成本统计中的 `TotalCost` 仍保留，用于账号成本和配额统计。

单位计算方式：

- Token：按输入、输出、缓存创建和缓存读取 Token 之和计费；图片 Token 已包含在对应聚合 Token 中，不重复计算。
- 图片：单价乘以图片数量。
- 视频：单价乘以视频计费秒数和视频数量；缺少有效时长时使用系统默认视频计费时长。
- 请求：每次匹配请求收取一次单价。

## 4. 配置存储

本功能**没有新增数据库表，也没有新增数据库迁移**。

配置复用现有 `settings` 键值表，按以下键名保存 JSON：

```text
satellite_billing_v1_{slug}
```

例如：

```text
satellite_billing_v1_canvas
```

没有保存过的应用默认使用：

```json
{
  "mode": "model",
  "rules": []
}
```

读取结果在服务内缓存 30 秒；更新成功后立即使缓存失效。数据库读写设置了 5 秒超时。

## 5. 后台 API

接口位于管理员设置路由下，要求管理员权限：

```http
GET /api/v1/admin/settings/satellite-billing
```

返回所有支持应用的配置：

```json
{
  "apps": {
    "canvas": {
      "mode": "model",
      "rules": []
    }
  }
}
```

更新单个应用：

```http
PUT /api/v1/admin/settings/satellite-billing/{slug}
Content-Type: application/json
```

请求体为 `SatelliteBillingConfig`。不支持的 slug、未知计费单位、重复规则和非法价格会返回 400。

## 6. 校验限制

- 每个应用最多 100 条规则。
- `target` 去除首尾空格后长度必须为 1 到 160 个字符。
- 价格必须是有限的、非负数，最大值为 `1,000,000,000`。
- 同一个 `target + unit` 组合不能重复。
- `millionTokens` 只允许在 `custom` 模式使用。
- `points` 模式的积分数量必须为整数。

前端和后端都会执行校验，后端校验是最终有效性边界。

## 7. 与卫星鉴权的关系

卫星请求经过现有卫星鉴权中间件后，已验证的卫星 slug 会写入请求上下文。计费逻辑只对受支持且已验证的 slug 应用卫星配置；普通 API 请求不会因为提交一个自定义请求头就获得卫星价格。

`agentapi` 不属于本模块支持名单，继续使用其既有账务桥接逻辑。

## 8. 主要实现位置

- 计费配置、持久化、缓存和报价计算：`sub2api/backend/internal/service/satellite_billing.go`
- 管理接口处理：`sub2api/backend/internal/handler/admin/setting_handler.go`
- 管理路由：`sub2api/backend/internal/server/routes/admin.go`
- 卫星上下文标记：`sub2api/backend/internal/pkg/ctxkey/ctxkey.go`、`sub2api/backend/internal/server/middleware/super_key_satellite.go`
- 网关扣费接入：`sub2api/backend/internal/service/gateway_usage_billing.go`、`sub2api/backend/internal/service/openai_gateway_usage.go`
- 管理页面：`sub2api/frontend/src/views/admin/SatelliteBillingView.vue`
- 前端 API 类型和请求：`sub2api/frontend/src/api/admin/settings.ts`
- 后端测试：`sub2api/backend/internal/service/satellite_billing_test.go`

