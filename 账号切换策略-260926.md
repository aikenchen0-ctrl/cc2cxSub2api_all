# 账号切换策略

日期：2026-09-26

## 触发规则

- 在 1 分钟滚动窗口内统计同一账号的上游请求失败次数。
- 失败次数达到大于等于 2 次时触发账号切换策略，也就是第 2 次失败立即触发。
- 失败计数不因单次成功请求重置，按滚动窗口自然过期。
- 客户端主动断开等无法归因于上游账号的请求取消不计入失败。

## 失败类型

所有可归因于上游账号的请求失败都参与计数，包括但不限于：

- HTTP `429`、`5xx` 等上游错误响应；
- HTTP `4xx` 等其他上游失败响应；
- 上游连接、传输和网关层错误。

## 触发后的处理

达到阈值后立即执行以下操作：

1. 将当前账号标记为临时不可调度。
2. 临时不可调度状态持续 1 分钟，之后自动解除。
3. 当前请求立即进入故障转移流程。
4. 从同一分组中选择其他正常、可调度账号继续请求。

## 状态持久化

- 账号临时不可调度状态写入账号状态存储，并同步运行时调度阻断。
- 数据库写入暂时失败时，当前进程仍立即保留 1 分钟的内存阻断，避免继续使用刚刚失败的账号。

## 参数摘要

| 参数 | 值 |
| --- | --- |
| 失败统计窗口 | 1 分钟滚动窗口 |
| 触发阈值 | 大于等于 2 次失败 |
| 触发时点 | 第 2 次失败 |
| 临时不可调度时长 | 1 分钟 |
| 切换范围 | 同一分组中的其他正常账号 |
